import { ClearFieldsDirective } from './clear-fields.directive';

describe('ClearFieldsDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearFieldsDirective();
    expect(directive).toBeTruthy();
  });
});
