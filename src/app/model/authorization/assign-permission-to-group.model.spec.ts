import { AssignPermissionToGroup } from './assign-permission-to-group.model';

describe('AssignPermissionToGroup', () => {
  it('should create an instance', () => {
    expect(new AssignPermissionToGroup()).toBeTruthy();
  });
});
