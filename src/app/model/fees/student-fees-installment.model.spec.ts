import { StudentFeesInstallment } from './student-fees-installment.model';

describe('StudentFeesInstallment', () => {
  it('should create an instance', () => {
    expect(new StudentFeesInstallment()).toBeTruthy();
  });
});
