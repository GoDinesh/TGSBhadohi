import { StudentFeesStructure } from './student-fees-structure.model';

describe('StudentFeesStructure', () => {
  it('should create an instance', () => {
    expect(new StudentFeesStructure()).toBeTruthy();
  });
});
