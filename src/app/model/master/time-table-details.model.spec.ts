import { TimeTableDetails } from './time-table-details.model';

describe('TimeTableDetails', () => {
  it('should create an instance', () => {
    expect(new TimeTableDetails()).toBeTruthy();
  });
});
