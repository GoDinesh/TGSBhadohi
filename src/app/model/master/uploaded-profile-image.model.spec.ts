import { UploadedProfileImage } from './uploaded-profile-image.model';

describe('UploadedProfileImage', () => {
  it('should create an instance', () => {
    expect(new UploadedProfileImage()).toBeTruthy();
  });
});
