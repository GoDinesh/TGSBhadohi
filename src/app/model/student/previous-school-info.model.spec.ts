import { PreviousSchoolInfo } from './previous-school-info.model';

describe('PreviousSchoolInfo', () => {
  it('should create an instance', () => {
    expect(new PreviousSchoolInfo()).toBeTruthy();
  });
});
